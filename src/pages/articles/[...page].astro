---
import ArticlesLayout from "@/layouts/articles-layout.astro";
import { getDefaultArticleListPaths } from "@/lib/i18n-routes";
import type { ArticlesListPageProps } from "@/lib/i18n-page-types";

export const getStaticPaths = getDefaultArticleListPaths;

export type Props = ArticlesListPageProps;

const { locale, page } = Astro.props;
---

<ArticlesLayout locale={locale} page={page} />
